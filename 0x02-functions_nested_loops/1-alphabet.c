#include <main.h>
/**
 * main - Entry point
 * Description : 'using puts function'
 * Return: Always (0)(Success)
 */
	void print_alphabet(void)
{
	char;

	for (c = 'a';
		c <= 'z'; c++)
	{_putchar(c);
	}
	putchar('\n');
}
